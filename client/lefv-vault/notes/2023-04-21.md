---
id: 2023-04-21
created_date: 21/04/2023
updated_date: 21/04/2023
type: note
---

# 2023-04-21

## 📝 Notes

21/04/2023:04:78

#dsu

received grub to generate training -->

trajectory generation and ir generation --> kinematic

emissive ir

reflective ir

kinematic generation

## Phase 3

Workflow scope

Screens

Clear direction

lull

state --> uat

```python


# Create a 3D grid of aspect and azimuth angles
aspect_grid, azimuth_grid = np.meshgrid(aspectAngles, azimuthAngles, indexing='ij')

# Rearrange the "irTrajectoryDataSet" array to have the aspect and azimuth axes first
irTrajectoryDataSet_reordered = np.moveaxis(irTrajectoryDataSet, [1, 2], [0, 1])

# Extract the irValues for all irPoints at once using advanced indexing
irValues = irTrajectoryDataSet_reordered[:, aspect_grid, azimuth_grid]

# Create a list of dictionaries with the irValues and corresponding labels
paramData = [{"value": [azimuthInd, len(aspectAngles) - 1 - aspectInd, irValue],
              "label": labels[len(aspectAngles) - 1 - aspectInd][azimuthInd]}
             for aspectInd in range(len(aspectAngles))
             for azimuthInd in range(len(azimuthAngles))
             for irValue, azimuthInd in zip(irValues[:, aspectInd, azimuthInd], range(len(azimuthAngles)))]

# Return the list of dictionaries
return paramData



# Create a 3D grid of aspect and azimuth angles
aspect_grid, azimuth_grid = np.meshgrid(aspectAngles, azimuthAngles, indexing='ij')

# Create a grid of reversed aspect indices
reverse_aspect_grid = len(aspectAngles) - 1 - aspect_grid

# Create a grid of labels using the aspect and azimuth grids
labels_grid = np.array(labels)[reverse_aspect_grid, azimuth_grid]

# Rearrange the "irTrajectoryDataSet" array to have the aspect and azimuth axes first
irTrajectoryDataSet_reordered = np.moveaxis(irTrajectoryDataSet, [1, 2], [0, 1])

# Extract the irValues for all irPoints at once using advanced indexing
irValues = irTrajectoryDataSet_reordered[:, aspect_grid, azimuth_grid]

# Create a list of dictionaries with the irValues and corresponding labels
paramData = [{"value": [azimuthInd, reverseAspectInd, irValue],
              "label": label}
             for irPoint, label, reverseAspectInd in zip(irValues, labels_grid.flatten(), reverse_aspect_grid.flatten())
             for irValue, azimuthInd in zip(irPoint, range(len(azimuthAngles)))]

# Return the list of dictionaries
return paramData


```

```python


data = []

# TODO: Replace triple nesting with np operation

for irPoint in np.asarray(trajectoryIrDataSet):

	paramData = []

	for aspectInd, aspect in enumerate(aspectAngles):

		reverseAspectInd = len(aspectAngles) - 1 - aspectInd

			for azimuthInd, azimuth in enumerate(azimuthAngles):

				irValue = irPoint[aspectInd, azimuthInd]

				paramData.append(

			{
			
			"value": [azimuthInd, reverseAspectInd, irValue],
			
			"label": labels[reverseAspectInd][azimuthInd],
			
			}

		)

	data.append(paramData)

```

length x height x width === length x row_count x col_count


## 🔗 Links

## **🏷️Tags**

- 
